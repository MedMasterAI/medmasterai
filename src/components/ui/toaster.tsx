'use client'

import { Toaster as SonnerToaster } from 'sonner'

export function Toaster() {
  return (
    // Puedes configurar posición, temas, richColors, etc.
    <SonnerToaster position="bottom-right" richColors />
  )
}
